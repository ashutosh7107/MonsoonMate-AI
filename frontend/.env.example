# Frontend environment variables
# These are the only variables that should be in the frontend
# NEVER put API keys here

# Backend API URL
# Development: (leave empty - Vite proxy handles it)
# Production (Render): https://your-render-backend.onrender.com/api
VITE_API_URL=
